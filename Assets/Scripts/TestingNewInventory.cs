using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class TestingNewInventory : MonoBehaviour
{
    public Text UiText;
    // Start is called before the first frame update
    void Start()
    {
       // UiText.text = PlayerPrefs.GetString("playerName") +" : "+ PlayerPrefs.GetInt("ArGun") + "  :  " + PlayerPrefs.GetInt("Difficulty");
    }

    // Update is called once per frame
    void Update()
    {
        
    }


    public void Appear()
    {
      //  UI.SetActive(true);
    }
}
